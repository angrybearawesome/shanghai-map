# CLAUDE.md — 상하이 여행 지도 작업 안내

> 이 문서는 이 저장소를 처음 보는 클로드 세션을 위한 온보딩이다.
> 사용자는 이 지도로 실제 상하이·우시 여행을 다녀왔거나 다녀올 예정이며,
> 여행 중에는 클로드 없이 UI만으로 썼다. 여행 후 유지보수·승격·기능 추가를
> 클로드에게 다시 시키는 것이 전제다.

## 한 줄 요약

`public/index.html` 하나가 **데이터이자 앱**이다. 외부 라이브러리·API 키·서버·빌드 없음.
`main` push → Cloudflare Workers 자동 배포 (~30초).

- 사이트: https://shanghai-map-202608.6011bear.workers.dev/
- 저장소: https://github.com/angrybearawesome/shanghai-map

## 절대 규칙 (깨면 안 되는 불변식)

1. **단일 HTML 파일 원칙** — JS/CSS 분리, 외부 CDN, API 키 도입 금지.
   사용자가 명시적으로 원칙 변경을 말하기 전엔 라우팅 API(고덕 등) 제안도 다시 꺼내지 않는다.
2. **카드가 곧 데이터** — `<main id="list">`의 `<article class="card">`가 장소 원본.
   핀·헤더 숫자·탭·범례·푸터는 전부 JS가 카드에서 계산한다. 숫자를 하드코딩하지 않는다.
3. **장소 번호(data-id)는 불변** — 삭제해도 재정렬하지 않는다. 방문 기록(localStorage)이
   번호로 묶여 있다.
4. **파생 값은 저장하지 않는다** — 루트·구간 추정·일차 번호는 렌더링 때마다 계산.
5. **수정 후 검증은 생략 금지** — `npm run verify`(로컬) → commit·push →
   `npm run verify:live`(배포 확인, "라이브 = 로컬 파일" 검사 포함). 실패 시 15초 간격 재시도.
6. 커밋 메시지는 한국어, 본문에 좌표 출처·판단 근거를 남기는 관례.

## 파일 지도

```
public/index.html    지도 본체 (HTML+CSS+JS 전부). 유일한 수정 대상
tools/verify.mjs     자동 검증 50+ 항목 (puppeteer-core, CHROME_PATH 조정 가능)
.claude/skills/      /place(장소 추가·삭제·승격) /coords(좌표 승급) /verify
docs/02-design/      설계 문서 — 아래 '문서 인덱스' 참조
wrangler.jsonc       배포 설정 (건드릴 일 거의 없음)
```

## 문서 인덱스 (작업 전에 해당 문서부터)

| 문서 | 내용 |
|---|---|
| `README.md` | 사용자 관점 전체 기능 설명 + localStorage 키 일람 |
| `docs/02-design/DESIGN-GUIDE.md` | **UI 판단 기준.** 새 UI를 만들기 전 §0의 3질문을 통과시킬 것 |
| `docs/02-design/trip-plan.design.md` | 여행 계획 기능 설계 (구간 추정·공유 포맷 v1/v2·익일 규칙) |
| `docs/02-design/user-places.design.md` | 내 장소(클로드 없이 추가)·숨기기·공유 v3 설계 |
| `.claude/skills/place/SKILL.md` | 장소 추가·삭제·**승격** 절차 (리서치 기준·신뢰도 매핑 포함) |

## index.html 내부 구조 (JS 모듈 순서)

1. **지도 엔진** — 타일(autonavi→OSM 폴백), project/unproject, 핀·팝업, 드래그·핀치
2. **데이터** — `cards`/`spots` (카드 DOM → 객체). `baseCards/baseSpots`는 정식 카드 스냅샷
3. **방문 기록** (`visits.v2`) + **일일 루트** (새벽 4시 경계)
4. **사용자 장소** (`uplaces.v1`) — 시작 시 `renderUplaces()`가 카드로 물질화해 spots에 합류.
   정식 장소 **숨기기**(`hidden.v1`)도 여기. 공식 숫자에서 내 장소는 제외(`s.user` 필터)
5. **여행 계획** (`plan.v1`) — 일차·스톱·구간 추정(직선거리×보정계수)·예약 별표(rsv)·
   출처 태그(day.src)·열람 잠금(WeakSet)·공유
6. **시작** — 레이아웃(2단은 innerWidth≥1024), 스플리터, 안내문 닫기

## 공유 링크 포맷 (`#plan=base64url(payload)`) — 전부 하위 호환 유지할 것

| 버전 | 판별 | 내용 |
|---|---|---|
| v1 | 디코드 결과가 `{`로 시작, `v:1` | JSON `{v:1, d:[[{t,i|n,g,r}]]}` |
| v2 | `2.` 접두 | 초압축 문자열: 일차 `;` 스톱 `,` / 스톱=`[HHMM][@id|~이름][+분[:교통편]][!]` |
| v3 | `{` + `v:3` | v1 구조 + `u:[{i,n,g,la,lo,m}]` (내 장소 동봉). 스톱이 uid를 참조할 때만 사용 |

가져오기: 항상 **받는 쪽 계획 뒤에 추가**(병합 없음), 보낸 사람 이름을 물어 `day.src`
태그(`이름 · 월/일`)를 붙인다. v3면 내 장소를 먼저 자동 추가(같은 uid 또는 같은 이름+60m
이내면 재사용).

## 자주 하는 작업

- **장소 추가/삭제**: `/place` 스킬 절차를 따른다 (general-purpose 에이전트로 웹 리서치,
  GCJ-02 좌표 교차 검증, 신뢰도→속성 매핑, 렌더링 스크린샷으로 핀 위치 육안 확인).
- **내 장소 승격**: 사용자가 "내 장소 ○○ 정식 등록" 또는 공유 링크를 주면 → 링크/설명에서
  이름·좌표를 얻고 → `/place` 리서치로 검증 → 정식 카드 추가. uid 방문 기록은 자동 이관
  안 됨(한계, 필요 시 사용자 안내).
- **좌표 승급**: `/coords` — approx/unknown 장소 리서치해 확정.
- **UI 변경**: DESIGN-GUIDE §0 통과 → CSS는 블록 주석으로 경계 → verify에 검사 추가.

## 사용자에 대해

- 한국어 사용. 숙소는 인민광장 건국 보취 호텔(15), 우시 숙소는 리신화푸 단지(26).
- 결정 이력: 이동 시간은 하이브리드 추정(API 금지), 계획 공유는 스냅샷(실시간 협업 아님),
  일차 병합 없음(태그 구분), 시간 피커 간격 이슈는 브라우저 특성으로 수용.
- 검증 스크린샷: `tools/last-verify.png`. 시각 확인이 필요한 변경은 puppeteer로
  전후 스크린샷을 찍어 보여주는 관례.
