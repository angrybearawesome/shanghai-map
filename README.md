# 상하이 여행 지도

단일 HTML 파일로 된 개인용 지도. 외부 라이브러리·API 키 없음.

- 사이트: https://shanghai-map-202608.6011bear.workers.dev/
- 저장소: https://github.com/angrybearawesome/shanghai-map
- 배포: Cloudflare Workers 정적 assets (`main` push → 자동 빌드)

```
public/index.html   ← 지도 본체. 이 파일만 고치면 됨
wrangler.jsonc      ← 배포 설정 (건드릴 일 거의 없음)
tools/verify.mjs    ← 자동 검증 (npm run verify / verify:live)
.claude/skills/     ← Claude Code 명령어: /place /coords /verify
```

Claude Code 명령어:

| 명령 | 하는 일 |
|---|---|
| `/place 추가 <이름>` | 좌표 리서치 → 카드 작성 → 검증 → push까지 자동 |
| `/place 삭제 <번호>` | 카드 제거 (번호 재정렬 없음 — 방문 기록 보호) |
| `/coords 02` 또는 `전부` | 점선 핀·목록만 장소를 리서치해 확정 좌표로 승급 |
| `/verify` | 로컬·라이브 자동 점검 (숫자 검산, 팝업, JS 오류, 배포 최신 여부) |

## 배포

Cloudflare Workers의 Git 연동을 써서, `main`에 push하면 자동 배포됩니다.

```
git add -A
git commit -m "장소 추가"
git push
```

배포 상황은 Cloudflare 대시보드 → Workers & Pages → shanghai-map-202608 → Deployments에서 봅니다.
연동 없이 CLI로 직접 올리려면 `npx wrangler deploy`.

## 장소 추가하는 법

**Claude Code를 쓰면**: `/place 추가 우캉맨션` 처럼 말하면 끝. 좌표 리서치부터
카드 작성·검증·push까지 자동으로 처리한다 (`.claude/skills/place/SKILL.md`).
삭제는 `/place 삭제 17`.

**손으로 하려면**: `public/index.html`에서 `<main id="list">` 안의 카드 한 덩이를 복사해 값만 바꿉니다.
지도 핀과 아이콘은 JS가 이 속성들을 읽어서 자동으로 만들어 주므로, 따로 손댈 곳이 없습니다.

```html
<article class="card" data-id="19" data-group="cafe" data-fix="ok"
         data-lat="31.2000" data-lon="121.4500" data-kw="검색용 상호 中文">
  <div class="c-head"><div class="idx">19</div>
    <div class="c-title"><h3 class="c-name">가게 이름 (Store Name)</h3><span class="c-tag">한줄 분류</span></div></div>
  <div class="addr-row"><div class="c-addr"><span class="a-txt">上海市... 주소</span></div></div>
  <p class="c-desc">설명.</p>
  <div class="links"><a href="#" data-act="search">이름으로 검색</a><a href="#" data-act="web">웹 지도</a><a href="#" data-act="apple">Apple 지도</a></div>
</article>
```

속성 의미:

| 속성 | 값 | 효과 |
|---|---|---|
| `data-id` | 두 자리 번호 | 배지·핀 번호. 중복 금지 |
| `data-group` | `cafe` / `spot` / `tea` | 색상(파랑/청록/자주)과 탭 분류 |
| `data-fix` | `ok` | 좌표 확인됨 → 채워진 핀 |
| | `approx` | 대략 위치 → 점선 핀 |
| | `unknown` | 위치 모름 |
| `data-nopin="1"` | (선택) | 지도에 핀을 만들지 않고 목록에만 표시 |
| `data-far="1"` | (선택) | '전체' 버튼이 화면을 맞출 때 제외 (쑤저우·푸동공항처럼 먼 곳) |
| `data-lat` `data-lon` | 숫자 | GCJ-02(고덕) 기준 좌표 |
| `data-kw` | 문자열 | 이름 검색에 쓰는 키워드. 중국어 상호가 있으면 그걸로 |

주소 밑에 경고 배지를 붙이려면 `a-txt` 뒤에 `<span class="flag">좌표 근사</span>`를 넣습니다.

카드만 추가하면 끝입니다. 헤더의 `N곳`, 문서 제목, 탭 라벨(`전체 18`), 범례 숫자,
헤더 안내문의 '주소를 확인하지 못한 N곳', 기록 바의 분모, 그룹 헤더 범위,
푸터의 번호 목록은 모두 카드에서 계산되므로 손댈 필요가 없습니다.
푸터 번호는 숫자순으로 정렬되고, 그룹 번호가 연속이 아니면 범위 대신 개수로 표시됩니다.

## 방문 기록

localStorage(`shcafemap.visits.v2`)에 방문 이벤트 목록 `[{id, ts}]`로 저장합니다.
**같은 곳을 다른 날 또 가면 이벤트가 한 줄 더 쌓입니다** (숙소처럼 매일 오가는 곳).
순번·루트는 매번 계산하므로 중간을 취소해도 번호가 자동으로 당겨집니다.

- 별 버튼은 '오늘' 기준 토글: 오늘 기록이 있으면 취소, 없으면 새 기록.
  잘못 누른 건 바로 다시 눌러 지우고, 지난 날 재방문은 그대로 쌓입니다.
- 재방문한 곳은 순번이 `3·9`처럼 병기됩니다 (카드·핀 동일).
- 지난 날 기록은 카드·팝업의 기록 줄에서 낱개로 취소합니다.
- 예전 v1(`{장소id: 시각}`) 데이터는 처음 열 때 자동으로 v2로 이관됩니다.
- 브라우저마다 따로 쌓이고, `file://`로 열면 저장이 안 되는 환경도 있습니다.

## 일일 루트

방문 기록을 날짜별로 묶어 만든 하루치 경로입니다. 별도 저장 없이 visits에서 매번 계산합니다.

- 방문한 날마다 기록 바 아래 `1일차 · 8/12` 칩이 생기고, 누르면 그날 방문 순서가
  지도에 점선으로 이어집니다. 루트에 없는 핀은 흐려지고, 칩 아래 걸음 목록이 나옵니다.
- `전체 루트`는 모든 날을 색을 바꿔가며 한 화면에 그립니다 (5색 순환).
- 점선은 방문 순서를 이은 직선일 뿐, 실제 도보·차량 경로가 아닙니다.
- 하루의 경계는 **새벽 4시**입니다. 자정 넘긴 술집 방문(00:40 등)은 전날 루트로 붙습니다.
- 지도에 핀이 없는 곳(`data-nopin`)은 걸음 목록에는 나오지만 선에서는 건너뜁니다.
- '복사' 버튼도 일차별로 묶어 내보냅니다.

## 여행 계획

상단 `지도 | 계획` 탭에서 출발 전에 일차별 동선을 짭니다. localStorage(`shcafemap.plan.v1`)에
`{days:[{stops:[{time, name, placeId, lat, lon, leg}]}]}` 구조로 저장합니다.
설계 문서: `docs/02-design/trip-plan.design.md`.

- 스톱마다 시간·장소를 입력합니다. 장소는 자유 텍스트(분당, 인천공항 등)도 되고,
  자동완성으로 지도의 장소를 고르면 좌표가 붙어 구간 계산·지도 보기가 됩니다.
- 좌표가 있는 두 스톱 사이는 직선거리 × 보정계수로 **도보·지하철·택시 소요 시간을
  자동 추정**해 보여줍니다 (참고용). 비행기·기차처럼 추정이 안 되는 구간은 '수정'으로
  소요 시간·교통편을 직접 입력하고, 직접 입력이 항상 추정보다 우선합니다.
- 수동 구간 정보(`leg`)는 **출발 스톱에 붙습니다**. 스톱을 이동하면 같이 따라갑니다.
- 앞뒤 스톱에 시간이 다 있으면 계획 간격과 이동 시간을 비교해 **여유/부족**을 표시합니다.
  다음 스톱 시간이 **새벽 4시 이전**이면 자정을 넘긴 것으로 보고 `(익일)`로 계산합니다
  (일일 루트의 하루 경계와 같은 규칙). 그 외의 시간 역순만 오류로 표시합니다.
- '주변'은 그 스톱에서 3km 안의 등록 장소를 가까운 순으로 추천하고, '다음에 추가'로
  바로 동선에 끼워 넣습니다. 고덕 주변 검색 링크도 있습니다.
- 장소칸 오른쪽 ★를 누르면 **예약 필요** 표시가 되고, 시간이 있으면 '예약 권장
  HH:MM까지(도착 1시간 전)' 칩이 붙습니다 (`st.rsv`). 공유 링크(v2의 `!` 접미)와
  '계획 복사' 텍스트에도 실립니다.
- '지도에 동선'은 그날 계획을 지도에 파란 점선으로 그립니다 (좌표 있는 곳만 연결).
- '계획 복사'는 방문 기록 복사처럼 텍스트로 내보냅니다. 계획도 브라우저별로 저장됩니다.
- **공유 링크**: 계획 전체를 `#plan=base64url` 해시에 담은 링크를 복사합니다. 서버 저장
  없음. 링크를 연 사람에게 확인을 받고 **그 사람 계획 뒤에 일차로 추가**됩니다 (기존
  계획은 보존). 보낸 뒤 원본을 고쳐도 상대에게 반영되지 않는 스냅샷 방식입니다.
  내용은 v2 초압축 포맷(`일차;` `스톱,` / 스톱=`HHMM@장소id|~이름[+분[:교통편]]`)이라
  지도 장소 위주 계획은 스톱당 10자 안팎입니다. 초기 v1(JSON) 링크도 계속 열립니다.
- **출처 태그**: 가져올 때 보낸 사람 이름을 물어(비워도 됨) 그 일차에 `철수 · 8/7`
  태그(`day.src`)를 붙입니다. 태그 있는 일차는 테두리도 구분됩니다. 병합은 하지 않는
  대신, 친구 일차를 참조해 내 일차를 보강하고 필요 없으면 그 일차만 삭제하는 흐름.
- 삭제는 세 단위: 스톱 ✕, 일차 '삭제', 하단 '전체 지우기' (모두 확인 후 진행).
- 일차는 헤더의 ↑↓로 순서 이동. 번호는 위치에서 매번 계산되므로 자동 재번호됩니다
  (공유받은 일차를 원하는 위치로 배치할 때 사용).

## 내 장소 — 클로드 없이 장소 추가 (여행 중 사용)

설계 문서: `docs/02-design/user-places.design.md`. 푸터의 **＋ 장소 추가** 버튼으로
브라우저에서 직접 장소를 만듭니다. localStorage(`shcafemap.uplaces.v1`)에 저장되고,
정식 카드와 같은 DOM으로 물질화되어 핀·팝업·방문 기록·계획·주변 추천이 모두 동작합니다.

- 이름·분류·메모 입력 + **'지도에서 찍기'로 지도를 한 번 눌러 위치 지정**
  (타일이 GCJ-02라 찍은 좌표가 곧 고덕 좌표 — 리서치 불필요). 위치 없이도 추가 가능(목록만).
- **링크·좌표 붙여넣기**도 됩니다: 애플지도 공유 링크(`coordinate=`/`ll=` 포함, 중국은
  GCJ-02), 고덕 공유 단축링크를 브라우저에서 연 뒤 주소창의 `amap.com/?p=…` 주소,
  또는 `31.22, 121.48` 생좌표. 고덕 단축링크(surl) 원문에는 좌표가 없어 바로는 안 됩니다.
  지도 권역(상하이~우시) 밖 좌표는 거부합니다.
- 내 장소는 **회색 핀 + ✦ 배지**로 정식 장소와 구분되고, '내 장소' 섹션에 모입니다.
- **공식 숫자(헤더 N곳·탭·범례·푸터)에는 포함되지 않습니다** — 공식 숫자는 파일 기준.
- 삭제는 카드의 '이 장소 삭제' (본인 브라우저 데이터라 자유).
- **계획 공유에 자동 동봉**: 계획 스톱이 내 장소를 참조하면 공유 링크가 v3(JSON) 포맷으로
  장소 데이터까지 싣고, 받는 쪽에 자동 추가됩니다 (같은 uid 또는 같은 이름+60m 이내면
  중복 추가 안 함). 내 장소가 없으면 기존 v2 초압축 링크 그대로.
- **승격(정식 등록)은 클로드 워크플로**: 여행 후 클로드에게 "내 장소 ○○를 정식 등록해줘"
  (또는 공유 링크 붙여넣기) → `/place` 절차로 리서치·검증 후 정식 카드가 됩니다.
  주의: 내 장소 uid로 쌓인 방문 기록은 정식 번호로 자동 이관되지 않습니다.

## 정식 장소 숨기기

정식 장소(공용 데이터)는 UI에서 삭제할 수 없고 **숨기기**만 됩니다 — 카드의 '숨기기' 링크.
숨긴 목록은 `shcafemap.hidden.v1`에 저장되고 푸터의 '숨긴 장소 N곳 · 모두 복원'으로
되돌립니다. 진짜 삭제(전원 반영)는 클로드 `/place 삭제 N`으로만 합니다.

## localStorage 키 일람

| 키 | 내용 |
|---|---|
| `shcafemap.visits.v2` | 방문 이벤트 `[{id, ts}]` (v1에서 자동 이관) |
| `shcafemap.plan.v1` | 여행 계획 `{days:[{src?, stops:[...]}]}` |
| `shcafemap.uplaces.v1` | 내 장소 `[{uid, name, group, memo, lat, lon}]` |
| `shcafemap.hidden.v1` | 숨긴 정식 장소 id 배열 |
| `shcafemap.tipclosed.v1` | 상단 안내문 닫음 여부 |
| `shcafemap.panelw.v1` | 2단 레이아웃 좌측 패널 폭(px) |
